#include "holberton.h"
/**
* _abs - function that computes the absolute value of an integer
* @m: The character to print
* Return: (0)
*/
int _abs(int m)
{
	if (m == 0)
	{
	return (m);
	}
	else if (m > 0)
	{
	return (m);
	}
	else
	{
	return (m * -1);
	}
	return (0);
}
